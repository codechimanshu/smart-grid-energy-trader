[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-grid"
version = "1.0.0"
description = "Smart Grid Energy Trader OpenEnv"
dependencies = [
    "gymnasium==0.29.1",
    "numpy==1.26.4",
    "openai==1.14.0"
]